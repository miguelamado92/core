<div class="bg-gray-900 p-4 lg:p6 xl:p-8 text-white grid grid-cols-1">
  {{#if status.success}}
  {{> blocks.event_success}}
  {{else}}
  {{> blocks.event_info}}

  {{#if status.error}}
  {{>blocks.error}}
  {{/if}}
  {{> blocks.event_form}}
  {{/if}}
</div>